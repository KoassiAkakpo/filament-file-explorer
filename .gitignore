/vendor/
composer.lock
.phpunit.result.cache
.phpunit.cache
.idea/
.vscode/
.DS_Store
Thumbs.db
node_modules/

# Graphify (local analysis only)
/graphify-out/
.graphify_labels.json
.graphify_root

# Published stubs during local QA (host apps publish into stubs/filament-file-explorer/).
# The package's own stubs/ directory must stay tracked — the make-* commands read it.
/stubs/filament-file-explorer/
